#include<stdio.h>
#include<math.h>
int main()
{
    int T,i,n,q;
    float s,res;
    scanf("%d",&T);
    for(i=0;i<T;i++)
    {
            scanf("%d",&n);

        s=sqrt(n);
        res=(int)(s);
    if(s==res)
    {
        printf("True
");
    }
    else
    {
        printf("False
");
    }
    }
    
    
}